# Managed by Ansible — credentials for the S3 cold archive (MinIO).
# object_store reads the standard AWS_* variables.
AWS_ACCESS_KEY_ID={{ minio_root_user }}
AWS_SECRET_ACCESS_KEY={{ minio_root_password }}
AWS_REGION=us-east-1
AWS_ALLOW_HTTP=true
RUST_LOG=info,tantivy=warn
