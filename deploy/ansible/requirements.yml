# Install with:  ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix      # synchronize (rsync)
    version: ">=1.5.0"
  - name: community.general  # ini_file, misc
    version: ">=8.0.0"
