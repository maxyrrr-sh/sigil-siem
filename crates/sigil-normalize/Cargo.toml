[package]
name = "sigil-normalize"
version = "0.0.0"
edition = "2021"
publish = false
description = "OCSF mapping and enrichment (DESIGN section 6)"

[dependencies]
sigil-core = { workspace = true }
serde_json = { workspace = true }
